import { FunctionOutlined } from "@ant-design/icons"
import { Button } from "antd"
import { memo } from "react"

export const PropsExpressionInput = memo(() => {
  return (
    <Button icon={<FunctionOutlined />} size="small"></Button>
  )
})