import { PredeinedValidators } from "../interfaces";
import { email } from "./email";
import { url } from "./url";
import { uuid } from "./uuid";

export const predifinedValidators: PredeinedValidators = {
  email,
  url,
  uuid
}