import { ResourceGroup } from "example-common";
import { LSTextMaterial } from "./basic";

export const largeScreenResources: ResourceGroup[] = [
  {
    titleKey: 'basic',
    items: [
      LSTextMaterial.componentName
    ]
  }
]