import { memo } from "react"

export enum LeftNavType {
  pages = "pages",
  compoents = "components",
  outline = "outline",
  history = "history",
}

export const Antd5Example = memo(() => {

  return (
    <div>
      逻辑编排引擎
    </div>
  )
})